using UnityEngine;
using System.Collections;

public class SoundBoard : MonoBehaviour {

	// Use this for initialization
	void Start () {
	GetComponent<Renderer>().material.color = Color.gray; //Makes SoundBoard gray
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
