using UnityEngine;
using System.Collections;

public class Black : MonoBehaviour {

	// Use this for initialization
	void Start () {
        GetComponent<Renderer>().material.color = Color.black; // Makes slide space black
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
